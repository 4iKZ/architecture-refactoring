# Shop (cycle fixture)

Toy order pipeline used by the architecture-refactoring skill evals.

- Run tests: `python -m unittest discover -s tests`
- Run demo: `python -m shop.main`
- Optional history for change-frequency evidence: `python setup_history.py`
